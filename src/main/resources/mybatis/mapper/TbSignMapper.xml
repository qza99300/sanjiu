<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atwangsi.user.dao.TbSignMapper">
  <resultMap id="BaseResultMap" type="com.atwangsi.user.model.TbSign">
    <id column="signId" jdbcType="INTEGER" property="signid" />
    <result column="userId" jdbcType="INTEGER" property="userid" />
    <result column="signDate" jdbcType="DATE" property="signdate" />
  </resultMap>
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Update_By_Example_Where_Clause">
    <where>
      <foreach collection="example.oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    signId, userId, signDate
  </sql>
  <select id="selectByExample" parameterType="com.atwangsi.user.model.TbSignExample" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    <include refid="Base_Column_List" />
    from tb_Sign
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from tb_Sign
    where signId = #{signid,jdbcType=INTEGER}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from tb_Sign
    where signId = #{signid,jdbcType=INTEGER}
  </delete>
  <delete id="deleteByExample" parameterType="com.atwangsi.user.model.TbSignExample">
    delete from tb_Sign
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </delete>
  <insert id="insert" parameterType="com.atwangsi.user.model.TbSign">
    insert into tb_Sign (signId, userId, signDate
      )
    values (#{signid,jdbcType=INTEGER}, #{userid,jdbcType=INTEGER}, #{signdate,jdbcType=DATE}
      )
  </insert>
  <insert id="insertSelective" parameterType="com.atwangsi.user.model.TbSign">
    insert into tb_Sign
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="signid != null">
        signId,
      </if>
      <if test="userid != null">
        userId,
      </if>
      <if test="signdate != null">
        signDate,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="signid != null">
        #{signid,jdbcType=INTEGER},
      </if>
      <if test="userid != null">
        #{userid,jdbcType=INTEGER},
      </if>
      <if test="signdate != null">
        #{signdate,jdbcType=DATE},
      </if>
    </trim>
  </insert>
  <select id="countByExample" parameterType="com.atwangsi.user.model.TbSignExample" resultType="java.lang.Long">
    select count(*) from tb_Sign
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>
  <update id="updateByExampleSelective" parameterType="map">
    update tb_Sign
    <set>
      <if test="record.signid != null">
        signId = #{record.signid,jdbcType=INTEGER},
      </if>
      <if test="record.userid != null">
        userId = #{record.userid,jdbcType=INTEGER},
      </if>
      <if test="record.signdate != null">
        signDate = #{record.signdate,jdbcType=DATE},
      </if>
    </set>
    <if test="_parameter != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
  <update id="updateByExample" parameterType="map">
    update tb_Sign
    set signId = #{record.signid,jdbcType=INTEGER},
      userId = #{record.userid,jdbcType=INTEGER},
      signDate = #{record.signdate,jdbcType=DATE}
    <if test="_parameter != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
  <update id="updateByPrimaryKeySelective" parameterType="com.atwangsi.user.model.TbSign">
    update tb_Sign
    <set>
      <if test="userid != null">
        userId = #{userid,jdbcType=INTEGER},
      </if>
      <if test="signdate != null">
        signDate = #{signdate,jdbcType=DATE},
      </if>
    </set>
    where signId = #{signid,jdbcType=INTEGER}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.atwangsi.user.model.TbSign">
    update tb_Sign
    set userId = #{userid,jdbcType=INTEGER},
      signDate = #{signdate,jdbcType=DATE}
    where signId = #{signid,jdbcType=INTEGER}
  </update>
  
  <select id="selectObj" resultMap="BaseResultMap">
  	SELECT *from tb_Sign where userId=#{0} and signDate=#{1}
  </select>
  
  <insert id="addsign" parameterType="com.atwangsi.user.model.TbSign">
  INSERT INTO tb_Sign (userId, signDate) VALUES (#{userid}, NOW());
  </insert>
</mapper>